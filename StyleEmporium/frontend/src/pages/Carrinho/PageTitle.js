import React from 'react';
// import './cart.scss'
const PageTitle = ({ title }) => {
    return (

        <div style={{ marginTop: '20px '}} className="page-title">{title}</div>
    );
};

export default PageTitle;
